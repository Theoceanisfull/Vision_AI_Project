Steps to run:

1. Put your model file here:
   clip_asl_ft.pth

2. Install dependencies:
   pip install -r requirements.txt

3. Run app:
   python asl_predict.py

4. Click "Choose Image" and select ASL image